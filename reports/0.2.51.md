# Claude Code 0.2.51

Compared with `0.2.50`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 228771 -> 229361 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

### Slash Commands
- ADDED: `/migrate-installer` [source-only]


## 0. Package Files
- CHANGED: `cli.js` (text, 228771 -> 229361 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

## 1. Slash Commands
### Changes
- ADDED: `/migrate-installer` [source-only]

### Current Inventory
- Built-in (20): /allowed-tools, /bug, /clear, /compact, /config, /cost, /doctor, /exit, /help, /init, /login, /logout, /mcp, /migrate-installer, /pr-comments, /release-notes, /review, /terminal-setup, /theme, /vim

## 2. Public Surface
### Models
- ADDED: `claude-code-mcp`

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: G > 0 ? `Found ${G} MCP server${G !== 1 ? "s" : ""} (use /mcp for status)` : "",
  - Command example: - /compact: Compact and continue the conversation. This is useful if the conversation is reaching the context limit
- MCP / Connectors (changed) [source-only]
  - Feature example: op: G.initiatorType ? `resource.${G.initiatorType}` : "resource.other",
  - Feature example: Mm1._addResourceSpans = Em1;
- Agents / Skills (changed) [source-only]
  - Feature example: class Kj1 extends Dj1.Agent {
  - Feature example: zj1.Agent = Kj1;
- Memory / Context (changed) [source-only]
  - Feature example: [OV.SEMANTIC_ATTRIBUTE_SENTRY_SOURCE]: this._latestRoute.context
  - Feature example: ? gV4(this._latestRoute.context)
- Permissions / Sandbox (changed) [source-only]
  - Config example: permission: u8(G.permission),
  - Config example: q3.PermissionDeniedError = KO;
- Models / Effort (changed) [source-only]
  - Config example: let { action: B, model: w } = Z;
  - Config example: (G.cpu_description = B.model),
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: let I = Dx.chrome,
  - Feature example: (d.color = Z.selectColor(C)),

## Official Changelog
- No official changelog entry found for this version
