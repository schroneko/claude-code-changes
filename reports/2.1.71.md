# Claude Code 2.1.71

Compared with `2.1.70`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 552282 -> 558284 lines)
- CHANGED: `package.json` (text, 33 -> 33 lines)
- REMOVED: `vendor/ripgrep/arm64-darwin/ripgrep.node` (binary, 5.9 MB)
- REMOVED: `vendor/ripgrep/arm64-linux/ripgrep.node` (binary, 4.4 MB)
- REMOVED: `vendor/ripgrep/arm64-win32/ripgrep.node` (binary, 3.8 MB)
- REMOVED: `vendor/ripgrep/x64-darwin/ripgrep.node` (binary, 5.9 MB)
- REMOVED: `vendor/ripgrep/x64-linux/ripgrep.node` (binary, 4.8 MB)
- REMOVED: `vendor/ripgrep/x64-win32/ripgrep.node` (binary, 6.6 MB)

### Slash Commands
- ADDED: `/heapdump` [source-only]

### Environment Variables
- ADDED: `CLAUDE_CODE_DEBUG_LOG_LEVEL`
- ADDED: `CLAUDE_CODE_TWO_STAGE_CLASSIFIER`


## 0. Package Files
- CHANGED: `cli.js` (text, 552282 -> 558284 lines)
- CHANGED: `package.json` (text, 33 -> 33 lines)
- REMOVED: `vendor/ripgrep/arm64-darwin/ripgrep.node` (binary, 5.9 MB)
- REMOVED: `vendor/ripgrep/arm64-linux/ripgrep.node` (binary, 4.4 MB)
- REMOVED: `vendor/ripgrep/arm64-win32/ripgrep.node` (binary, 3.8 MB)
- REMOVED: `vendor/ripgrep/x64-darwin/ripgrep.node` (binary, 5.9 MB)
- REMOVED: `vendor/ripgrep/x64-linux/ripgrep.node` (binary, 4.8 MB)
- REMOVED: `vendor/ripgrep/x64-win32/ripgrep.node` (binary, 6.6 MB)

## 1. CLI Commands
### Changes
- No changes detected

### Current Inventory
- Top-level (9): claude agents, claude auth, claude doctor, claude install [target], claude mcp, claude plugin, claude remote-control (hidden; aliases: claude rc), claude setup-token, claude update (aliases: claude upgrade)
- auth (3): claude auth login, claude auth logout, claude auth status
- mcp (8): claude mcp add <name> <commandOrUrl> [args...], claude mcp add-from-claude-desktop, claude mcp add-json <name> <json>, claude mcp get <name>, claude mcp list, claude mcp remove <name>, claude mcp reset-project-choices, claude mcp serve
- plugin (8): claude plugin disable [plugin], claude plugin enable <plugin>, claude plugin install <plugin> (aliases: claude plugin i <plugin>), claude plugin list, claude plugin marketplace, claude plugin uninstall <plugin> (aliases: claude plugin remove <plugin>, claude plugin rm <plugin>), claude plugin update <plugin>, claude plugin validate <path>
- plugin marketplace (4): claude plugin marketplace add <source>, claude plugin marketplace list, claude plugin marketplace remove <name> (aliases: claude plugin marketplace rm <name>), claude plugin marketplace update [name]

## 2. Slash Commands
### Changes
- ADDED: `/heapdump` [source-only]
- ADDED: `/loop` [official]

### Current Inventory
- Built-in (68): /add-dir, /agents, /btw, /chrome, /clear, /color, /commit, /commit-push-pr, /compact, /config, /context, /copy, /cost, /desktop, /doctor, /exit, /export, /extra-usage, /fast, /feedback, /files, /fork, /heapdump, /help, /hooks, /ide, /init, /init-verifiers, /insights, /install-github-app, /install-slack-app, /keybindings, /login, /logout, /mcp, /mobile, /model, /output-style, /passes, /permissions, /plan, /plugin, /privacy-settings, /rate-limit-options, /release-notes, /reload-plugins, /remote-control, /remote-env, /rename, /resume, /rewind, /sandbox, /session, /skills, /stats, /status, /statusline, /stickers, /tag, /tasks, /terminal-setup, /theme, /think-back, /thinkback-play, /upgrade, /usage, /vim, /voice
- Plugin-backed (3): /code-review, /pr-comments, /security-review
- Inferred (2): /loop [inferred/medium], /memory [inferred/medium]

## 3. Public Surface
### Models
- No changes detected

### Environment Variables
- ADDED: `CLAUDE_CODE_DEBUG_LOG_LEVEL`
- ADDED: `CLAUDE_CODE_TWO_STAGE_CLASSIFIER`

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 4. Capability Signals
- Slash / Workflows (changed)
  - Official: Added `/loop` command to run a prompt or slash command on a recurring interval (e.g. `/loop 5m check the deploy`)
  - Official: Fixed forked conversations (`/fork`) sharing the same plan file, which caused plan edits in one fork to overwrite the other
  - Source: Slash command added: /loop
  - Command example: z && o3.createElement(T, { dimColor: !0 }, "Plan saved to: ", _, " · /plan to edit"),
  - Command example: let P = `${X} ${M1.dim("· /permissions to manage")}`;
- MCP / Connectors (changed)
  - Official: Fixed startup UI freeze when many claude.ai proxy connectors refresh an expired OAuth token simultaneously
  - Official: Fixed claude.ai connectors failing to reconnect after OAuth token refresh
  - Feature example: setOauthTokenFromFd: () => h46,
  - Feature example: getOauthTokenFromFd: () => sg1,
- Plugins / Marketplace (changed)
  - Official: Fixed plugin installations being lost when running multiple Claude Code instances
  - Official: Fixed `/plugin marketplace update` failing with merge conflicts when the marketplace is pinned to a branch/tag ref
  - Command example: return `The name '${A}' is reserved for official Anthropic marketplaces. Only repositories from 'github.com/${L31}/' can use this name.`;
  - Feature example: return `The name '${A}' is reserved for official Anthropic marketplaces and can only be used with GitHub sources from the '${L31}' organization.`;
- Agents / Skills (changed)
  - Official: Fixed background agent completion notifications missing the output file path, which made it difficult for parent agents to recover agent results after context compaction
  - Command example: N.set("User-Agent", "axios/" + hq6, !1);
  - Command example: headers: { "Content-Type": "application/json", "User-Agent": mO(), ...K.headers },
- Memory / Context (changed)
  - Official: Fixed the Read tool putting oversized images into context when image processing failed, breaking subsequent turns in long image-heavy sessions
  - Official: Fixed background agent completion notifications missing the output file path, which made it difficult for parent agents to recover agent results after context compaction
  - Feature example: sampling: Y4({ context: O0.optional(), tools: O0.optional() }).optional(),
  - Feature example: context: Y4({ arguments: z$(n1(), n1()).optional() }).optional(),
- Permissions / Sandbox (changed)
  - Official: Added `fmt`, `comm`, `cmp`, `numfmt`, `expr`, `test`, `printf`, `getconf`, `seq`, `tsort`, and `pr` to the bash auto-approval allowlist
  - Official: Fixed false-positive permission prompts for compound bash commands containing heredoc commit messages
  - Config example: PERMISSION_DENIED: obA,
  - Config example: PERMISSION_MODES: () => hZ,
- Models / Effort (changed)
  - Official: Fixed prompting for `cd <cwd> && git ...` on Windows when the model uses a mingw-style path
  - Config example: let z = q.model ?? b8(this, FD, "f").params.model,
  - Feature example: (SO3 = ["claude-opus-4-6"]));
- IDE / UI / Voice / Remote (changed)
  - Official: Added `voice:pushToTalk` keybinding to make the voice activation key rebindable in `keybindings.json` (default: space) — modifier+letter combos like `meta+k` have zero typing interference
  - Official: Fixed a 5–8 second startup freeze for users with voice mode enabled, caused by CoreAudio initialization blocking the main thread after system wake
  - Feature example: return u7() && u1.clientType !== "claude-vscode";
  - Feature example: theme: Tf(["light", "dark"]).optional(),

## Official Changelog
- Added `/loop` command to run a prompt or slash command on a recurring interval (e.g. `/loop 5m check the deploy`)
- Added cron scheduling tools for recurring prompts within a session
- Added `voice:pushToTalk` keybinding to make the voice activation key rebindable in `keybindings.json` (default: space) — modifier+letter combos like `meta+k` have zero typing interference
- Added `fmt`, `comm`, `cmp`, `numfmt`, `expr`, `test`, `printf`, `getconf`, `seq`, `tsort`, and `pr` to the bash auto-approval allowlist
- Fixed stdin freeze in long-running sessions where keystrokes stop being processed but the process stays alive
- Fixed a 5–8 second startup freeze for users with voice mode enabled, caused by CoreAudio initialization blocking the main thread after system wake
- Fixed startup UI freeze when many claude.ai proxy connectors refresh an expired OAuth token simultaneously
- Fixed forked conversations (`/fork`) sharing the same plan file, which caused plan edits in one fork to overwrite the other
- Fixed the Read tool putting oversized images into context when image processing failed, breaking subsequent turns in long image-heavy sessions
- Fixed false-positive permission prompts for compound bash commands containing heredoc commit messages
- Fixed plugin installations being lost when running multiple Claude Code instances
- Fixed claude.ai connectors failing to reconnect after OAuth token refresh
- Fixed claude.ai MCP connector startup notifications appearing for every org-configured connector instead of only previously connected ones
- Fixed background agent completion notifications missing the output file path, which made it difficult for parent agents to recover agent results after context compaction
- Fixed duplicate output in Bash tool error messages when commands exit with non-zero status
- Fixed Chrome extension auto-detection getting permanently stuck on "not installed" after running on a machine without local Chrome
- Fixed `/plugin marketplace update` failing with merge conflicts when the marketplace is pinned to a branch/tag ref
- Fixed `/plugin marketplace add owner/repo@ref` incorrectly parsing `@` — previously only `#` worked as a ref separator, causing undiagnosable errors with `strictKnownMarketplaces`
- Fixed duplicate entries in `/permissions` Workspace tab when the same directory is added with and without a trailing slash
- Fixed `--print` hanging forever when team agents are configured — the exit loop no longer waits on long-lived `in_process_teammate` tasks
- Fixed "❯ Tool loaded." appearing in the REPL after every `ToolSearch` call
- Fixed prompting for `cd <cwd> && git ...` on Windows when the model uses a mingw-style path
- Improved startup time by deferring native image processor loading to first use
- Improved bridge session reconnection to complete within seconds after laptop wake from sleep, instead of waiting up to 10 minutes
- Improved `/plugin uninstall` to disable project-scoped plugins in `.claude/settings.local.json` instead of modifying `.claude/settings.json`, so changes don't affect teammates
- Improved plugin-provided MCP server deduplication — servers that duplicate a manually-configured server (same command/URL) are now skipped, preventing duplicate connections and tool sets. Suppressions are shown in the `/plugin` menu.
- Updated `/debug` to toggle debug logging on mid-session, since debug logs are no longer written by default
- Removed startup notification noise for unauthenticated org-registered claude.ai connectors
