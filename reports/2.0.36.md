# Claude Code 2.0.36

Compared with `2.0.35`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 446411 -> 447092 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

### Slash Commands
- ADDED: `/extra-usage` [source-only]


## 0. Package Files
- CHANGED: `cli.js` (text, 446411 -> 447092 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

## 1. Slash Commands
### Changes
- ADDED: `/extra-usage` [source-only]

### Current Inventory
- Built-in (42): /add-dir, /agents, /bashes, /clear, /compact, /config, /cost, /doctor, /exit, /export, /extra-usage, /feedback, /files, /help, /hooks, /ide, /init, /install-github-app, /login, /logout, /mcp, /migrate-installer, /model, /output-style, /permissions, /plugin, /pr-comments, /privacy-settings, /release-notes, /resume, /review, /rewind, /sandbox, /security-review, /status, /statusline, /stickers, /terminal-setup, /todos, /upgrade, /usage, /vim
- Inferred (1): /memory [inferred/low]

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: let Z = W8A("warning") ?? "Run /compact to compact & continue";
  - Command example: r0.createElement(w, { dimColor: !0 }, " · /mcp"),
- MCP / Connectors (changed) [source-only]
  - Command example: let B = A.match(/^mcp-cli\s+(call|read)\s+([a-zA-Z0-9_-]+)\/([a-zA-Z0-9_-]+)(?:\s+(.+))?$/);
  - Feature example: NoAuthSigner: () => I14,
- Plugins / Marketplace (changed) [source-only]
  - Config example: CreateMarketplaceModelEndpointCommand: () => h1B,
  - Config example: DeleteMarketplaceModelEndpointCommand: () => r1B,
- Agents / Skills (changed) [source-only]
  - Command example: R.set("User-Agent", "axios/" + El, !1);
  - Feature example: return new MY1.Agent({ keepAlive: !0, maxSockets: 50, ...Y });
- Memory / Context (changed) [source-only]
  - Feature example: ((this.props = A), (this.context = B), (this.refs = pZ0), (this.updater = Q || dZ0));
  - Feature example: let Y = hJ9(A, { context: "initialize", markers: G.slice(), statsigOptions: I });
- Permissions / Sandbox (changed) [source-only]
  - Config example: b3.PermissionDeniedError = fWA;
  - Command example: kB.createElement(w, { bold: !0, color: "permission" }, "Submit Feedback / Bug Report"),
- Models / Effort (changed) [source-only]
  - Config example: (I = AU[this.model].channels),
  - Config example: ((this.model = B || "rgb"), (I = AU[this.model].channels));
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: else if ($$("color") || $$("colors") || $$("color=true") || $$("color=always")) IMA = 1;
  - Feature example: (SG.color.ansi = nO0()),

## Official Changelog
- Fixed: DISABLE_AUTOUPDATER environment variable now properly disables package manager update notifications
- Fixed queued messages being incorrectly executed as bash commands
- Fixed input being lost when typing while a queued message is processed
