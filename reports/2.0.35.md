# Claude Code 2.0.35

Compared with `2.0.34`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 446438 -> 446411 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

### Environment Variables
- ADDED: `CLAUDE_CODE_EXIT_AFTER_STOP_DELAY`


## 0. Package Files
- CHANGED: `cli.js` (text, 446438 -> 446411 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

## 1. Slash Commands
### Changes
- No changes detected

### Current Inventory
- Built-in (41): /add-dir, /agents, /bashes, /clear, /compact, /config, /cost, /doctor, /exit, /export, /feedback, /files, /help, /hooks, /ide, /init, /install-github-app, /login, /logout, /mcp, /migrate-installer, /model, /output-style, /permissions, /plugin, /pr-comments, /privacy-settings, /release-notes, /resume, /review, /rewind, /sandbox, /security-review, /status, /statusline, /stickers, /terminal-setup, /todos, /upgrade, /usage, /vim
- Inferred (1): /memory [inferred/low]

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- ADDED: `CLAUDE_CODE_EXIT_AFTER_STOP_DELAY`

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: let Z = Y8A("warning") ?? "Run /compact to compact & continue";
  - Feature example: content: Jz({ inputString: `Unknown slash command: ${C}`, precedingInputBlocks: B }),
- MCP / Connectors (changed) [source-only]
  - Feature example: NoAuthSigner: () => OA4,
  - Feature example: signer: new vG4.NoAuthSigner(),
- Plugins / Marketplace (changed) [source-only]
  - Config example: CreateMarketplaceModelEndpointCommand: () => z1B,
  - Config example: DeleteMarketplaceModelEndpointCommand: () => j1B,
- Agents / Skills (changed) [source-only]
  - Command example: T.set("User-Agent", "axios/" + Vl, !1);
  - Feature example: return new KY1.Agent({ keepAlive: !0, maxSockets: 50, ...Y });
- Memory / Context (changed) [source-only]
  - Feature example: ((this.props = A), (this.context = B), (this.refs = LZ0), (this.updater = Q || qZ0));
  - Feature example: let Y = YJ9(A, { context: "initialize", markers: G.slice(), statsigOptions: I });
- Permissions / Sandbox (changed)
  - Official: Migrated `ignorePatterns` from project config to deny permissions in the localSettings.
  - Config example: - ALWAYS use ${wH} for search tasks. NEVER invoke \`grep\` or \`rg\` as a ${x4} command. The ${wH} tool has been optimized for correct permissions and access.
  - Config example: v3.PermissionDeniedError = kWA;
- Models / Effort (changed) [source-only]
  - Config example: (I = ez[this.model].channels),
  - Config example: ((this.model = B || "rgb"), (I = ez[this.model].channels));
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: else if ($$("color") || $$("colors") || $$("color=true") || $$("color=always")) rLA = 1;
  - Feature example: (SG.color.close = "\x1B[39m"),

## Official Changelog
- Improve fuzzy search results when searching commands
- Improved VS Code extension to respect `chat.fontSize` and `chat.fontFamily` settings throughout the entire UI, and apply font changes immediately without requiring reload
- Added `CLAUDE_CODE_EXIT_AFTER_STOP_DELAY` environment variable to automatically exit SDK mode after a specified idle duration, useful for automated workflows and scripts
- Migrated `ignorePatterns` from project config to deny permissions in the localSettings.
- Fixed menu navigation getting stuck on items with empty string or other falsy values (e.g., in the `/hooks` menu)
