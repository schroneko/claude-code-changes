# Claude Code 2.0.15

Compared with `2.0.14`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 417798 -> 419236 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)
- CHANGED: `sdk.d.ts` (text, 424 -> 435 lines)
- CHANGED: `sdk.mjs` (text, 14830 -> 14830 lines)


## 0. Package Files
- CHANGED: `cli.js` (text, 417798 -> 419236 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)
- CHANGED: `sdk.d.ts` (text, 424 -> 435 lines)
- CHANGED: `sdk.mjs` (text, 14830 -> 14830 lines)

## 1. Slash Commands
### Changes
- No changes detected

### Current Inventory
- Built-in (41): /add-dir, /agents, /bashes, /clear, /compact, /config, /cost, /doctor, /exit, /export, /feedback, /files, /help, /hooks, /ide, /init, /install-github-app, /login, /logout, /mcp, /migrate-installer, /model, /output-style, /output-style:new, /permissions, /plugin, /pr-comments, /privacy-settings, /release-notes, /resume, /review, /rewind, /sandbox, /security-review, /status, /statusline, /terminal-setup, /todos, /upgrade, /usage, /vim
- Inferred (1): /memory [inferred/low]

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: `Marketplace '${Q.name}' is already installed. Please remove it first using '/plugin marketplace remove ${Q.name}' if you want to re-install it.`,
  - Feature example: content: az({ inputString: `Unknown slash command: ${D}`, precedingInputBlocks: B }),
- MCP / Connectors (changed) [source-only]
  - Command example: let B = `${c4().BASE_API_URL}/api/oauth/claude_cli/client_data`;
  - Feature example: NoAuthSigner: () => LO9,
- Plugins / Marketplace (changed) [source-only]
  - Config example: CreateMarketplaceModelEndpointCommand: () => NpA,
  - Config example: DeleteMarketplaceModelEndpointCommand: () => xpA,
- Agents / Skills (changed) [source-only]
  - Command example: T.set("User-Agent", "axios/" + Bd, !1);
  - Feature example: (this.agent = i),
- Memory / Context (changed) [source-only]
  - Feature example: _xQ = Symbol.for("react.context"),
  - Feature example: ((this.props = A), (this.context = B), (this.refs = qe0), (this.updater = Q || $e0));
- Permissions / Sandbox (changed) [source-only]
  - Config example: return { session_id: M2(), transcript_path: jN1(), cwd: YA(), permission_mode: A };
  - Config example: - ALWAYS use ${DK} for search tasks. NEVER invoke \`grep\` or \`rg\` as a ${_6} command. The ${DK} tool has been optimized for correct permissions and access.
- Models / Effort (changed) [source-only]
  - Config example: (Z = vC[this.model].channels),
  - Config example: ((this.model = B || "rgb"), (Z = vC[this.model].channels));
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: else if (v$("color") || v$("colors") || v$("color=true") || v$("color=always")) Q$1 = 1;
  - Feature example: (AG.color.close = "\x1B[39m"),

## Official Changelog
- Fixed bug with resuming where previously created files needed to be read again before writing
- Fixed bug with `-p` mode where @-mentioned files needed to be read again before writing
