# Claude Code 2.0.32

Compared with `2.0.31`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 440791 -> 441105 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)
- CHANGED: `sdk-tools.d.ts` (text, 1493 -> 1497 lines)

### Slash Commands
- ADDED: `/stickers` [source-only]

### Environment Variables
- ADDED: `CLAUDE_CODE_MAX_TOOL_USE_CONCURRENCY`
- REMOVED: `CLAUDE_CODE_FORCE_HALLOWEEN`


## 0. Package Files
- CHANGED: `cli.js` (text, 440791 -> 441105 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)
- CHANGED: `sdk-tools.d.ts` (text, 1493 -> 1497 lines)

## 1. Slash Commands
### Changes
- ADDED: `/stickers` [source-only]

### Current Inventory
- Built-in (41): /add-dir, /agents, /bashes, /clear, /compact, /config, /cost, /doctor, /exit, /export, /feedback, /files, /help, /hooks, /ide, /init, /install-github-app, /login, /logout, /mcp, /migrate-installer, /model, /output-style, /permissions, /plugin, /pr-comments, /privacy-settings, /release-notes, /resume, /review, /rewind, /sandbox, /security-review, /status, /statusline, /stickers, /terminal-setup, /todos, /upgrade, /usage, /vim
- Inferred (1): /memory [inferred/low]

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- ADDED: `CLAUDE_CODE_MAX_TOOL_USE_CONCURRENCY`
- REMOVED: `CLAUDE_CODE_FORCE_HALLOWEEN`

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: return `Run /compact or /model ${B.alias} to continue`;
  - Command example: let Z = p8A("warning") ?? "Run /compact to compact & continue";
- MCP / Connectors (changed) [source-only]
  - Feature example: return QB.oauthTokenFromFd;
  - Feature example: QB.oauthTokenFromFd = A;
- Plugins / Marketplace (changed) [source-only]
  - Config example: CreateMarketplaceModelEndpointCommand: () => Se0,
  - Config example: DeleteMarketplaceModelEndpointCommand: () => de0,
- Agents / Skills (changed) [source-only]
  - Command example: T.set("User-Agent", "axios/" + hp, !1);
  - Feature example: return new EY1.Agent({ keepAlive: !0, maxSockets: 50, ...Y });
- Memory / Context (changed) [source-only]
  - Feature example: ((this.props = A), (this.context = B), (this.refs = k80), (this.updater = Q || S80));
  - Feature example: let Y = d69(A, { context: "initialize", markers: G.slice(), statsigOptions: I });
- Permissions / Sandbox (changed) [source-only]
  - Config example: - ALWAYS use ${fE} for search tasks. NEVER invoke \`grep\` or \`rg\` as a ${g4} command. The ${fE} tool has been optimized for correct permissions and access.
  - Config example: S3.PermissionDeniedError = IYA;
- Models / Effort (changed) [source-only]
  - Config example: (I = gU[this.model].channels),
  - Config example: ((this.model = B || "rgb"), (I = gU[this.model].channels));
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: return QB.isNonInteractiveSession && QB.clientType !== "claude-vscode";
  - Feature example: else if (I$("color") || I$("colors") || I$("color=true") || I$("color=always")) bNA = 1;

## Official Changelog
- Un-deprecate output styles based on community feedback
- Added `companyAnnouncements` setting for displaying announcements on startup
- Fixed hook progress messages not updating correctly during PostToolUse hook execution
