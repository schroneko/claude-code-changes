# Claude Code 2.1.19

Compared with `2.1.18`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 521013 -> 522537 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)
- CHANGED: `sdk-tools.d.ts` (text, 1566 -> 1574 lines)

### Environment Variables
- ADDED: `CLAUDE_CODE_EMIT_TOOL_USE_SUMMARIES`
- ADDED: `CLAUDE_CODE_ENABLE_TASKS`

### SDK Tools
- CHANGED: `AgentInput` (+allowed_tools?)
- REMOVED: `KillShellInput`
- ADDED: `TaskStopInput`


## 0. Package Files
- CHANGED: `cli.js` (text, 521013 -> 522537 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)
- CHANGED: `sdk-tools.d.ts` (text, 1566 -> 1574 lines)

## 1. Slash Commands
### Changes
- No changes detected

### Current Inventory
- Built-in (59): /add-dir, /agents, /btw, /chrome, /clear, /color, /compact, /config, /context, /cost, /discover, /doctor, /exit, /export, /extra-usage, /feedback, /files, /fork, /help, /hooks, /ide, /init, /install-github-app, /install-slack-app, /keybindings, /login, /logout, /mcp, /mobile, /model, /output-style, /passes, /permissions, /plan, /plugin, /privacy-settings, /rate-limit-options, /release-notes, /remote-env, /rename, /resume, /rewind, /sandbox, /session, /skills, /stats, /status, /statusline, /stickers, /tag, /tasks, /terminal-setup, /theme, /think-back, /thinkback-play, /todos, /upgrade, /usage, /vim
- Plugin-backed (3): /code-review, /pr-comments, /security-review
- Inferred (4): /memory [inferred/medium], /review [inferred/medium], /share [inferred/medium], /teleport [inferred/medium]

## 2. Public Surface
### Models
- REMOVED: `claude-code-keybindings`

### Environment Variables
- ADDED: `CLAUDE_CODE_EMIT_TOOL_USE_SUMMARIES`
- ADDED: `CLAUDE_CODE_ENABLE_TASKS`

### Settings
- No changes detected

### SDK Tools
- CHANGED: `AgentInput` (+allowed_tools?)
- REMOVED: `KillShellInput`
- ADDED: `TaskStopInput`

## 3. Capability Signals
- Slash / Workflows (changed)
  - Official: Fixed `/rename` and `/tag` not updating the correct session when resuming from a different directory (e.g., git worktrees)
  - Command example: tryItPrompt: "Type /resume to continue a past conversation",
  - Command example: tryItPrompt: "Type /clear for a fresh conversation",
- MCP / Connectors (changed) [source-only]
  - Feature example: return R6.oauthTokenFromFd;
  - Feature example: R6.oauthTokenFromFd = A;
- Plugins / Marketplace (changed) [source-only]
  - Config example: _Fq = "CreateMarketplaceModelEndpoint",
  - Command example: { [t8]: ["POST", "/marketplace-model/endpoints", 200] },
- Agents / Skills (changed)
  - Official: Fixed agent list displaying "Sonnet (default)" instead of "Inherit (default)" for agents without an explicit model setting
  - Command example: T.set("User-Agent", "axios/" + kAA, !1);
  - Feature example: class CP8 extends kP8.Agent {
- Memory / Context (changed) [source-only]
  - Config example: illegalBy: { msg: O1.message, context: $A.slice(j1 - 100, j1 + 100), mode: O1.mode },
  - Feature example: (this._latestRoute.context = O),
- Permissions / Sandbox (changed)
  - Official: Changed skills without additional permissions or hooks to be allowed without requiring approval
  - Config example: let O = { ...z, permissions: { ...w, [K]: [...H, ...X] } },
  - Config example: `Applying permission update: Adding ${K.rules.length} ${K.behavior} rule(s) to destination '${K.destination}': ${UA(q)}`,
- Models / Effort (changed)
  - Official: Fixed agent list displaying "Sonnet (default)" instead of "Inherit (default)" for agents without an explicit model setting
  - Config example: console.warn(`The model '${Y.model}' is deprecated and will reach end-of-life on ${zk8[Y.model]}
  - Config example: let w = CtA[Y.model] ?? void 0;
- IDE / UI / Voice / Remote (changed)
  - Official: [VSCode] Enabled session forking and rewind functionality for all users
  - Feature example: return h7() && R6.clientType !== "claude-vscode";
  - Feature example: (GJ.color.close = "\x1B[39m"),

## Official Changelog
- Added env var `CLAUDE_CODE_ENABLE_TASKS`, set to `false` to keep the old system temporarily
- Added shorthand `$0`, `$1`, etc. for accessing individual arguments in custom commands
- Fixed crashes on processors without AVX instruction support
- Fixed dangling Claude Code processes when terminal is closed by catching EIO errors from `process.exit()` and using SIGKILL as fallback
- Fixed `/rename` and `/tag` not updating the correct session when resuming from a different directory (e.g., git worktrees)
- Fixed resuming sessions by custom title not working when run from a different directory
- Fixed pasted text content being lost when using prompt stash (Ctrl+S) and restore
- Fixed agent list displaying "Sonnet (default)" instead of "Inherit (default)" for agents without an explicit model setting
- Fixed backgrounded hook commands not returning early, potentially causing the session to wait on a process that was intentionally backgrounded
- Fixed file write preview omitting empty lines
- Changed skills without additional permissions or hooks to be allowed without requiring approval
- Changed indexed argument syntax from `$ARGUMENTS.0` to `$ARGUMENTS[0]` (bracket syntax)
- [SDK] Added replay of `queued_command` attachment messages as `SDKUserMessageReplay` events when `replayUserMessages` is enabled
- [VSCode] Enabled session forking and rewind functionality for all users
