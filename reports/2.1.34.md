# Claude Code 2.1.34

Compared with `2.1.33`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 518031 -> 518038 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)


## 0. Package Files
- CHANGED: `cli.js` (text, 518031 -> 518038 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

## 1. Slash Commands
### Changes
- No changes detected

### Current Inventory
- Built-in (60): /add-dir, /agents, /btw, /chrome, /clear, /color, /compact, /config, /context, /copy, /cost, /doctor, /exit, /export, /extra-usage, /feedback, /files, /fork, /help, /hooks, /ide, /init, /insights, /install-github-app, /install-slack-app, /keybindings, /login, /logout, /mcp, /mobile, /model, /output-style, /passes, /permissions, /plan, /plugin, /privacy-settings, /rate-limit-options, /release-notes, /remote-env, /rename, /resume, /rewind, /sandbox, /session, /skills, /stats, /status, /statusline, /stickers, /tag, /tasks, /terminal-setup, /theme, /think-back, /thinkback-play, /todos, /upgrade, /usage, /vim
- Plugin-backed (3): /code-review, /pr-comments, /security-review
- Inferred (1): /memory [inferred/medium]

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: "@babel/plugin-proposal-private-methods": "^7.18.6",
  - Command example: REQUIREMENTS FOR SUMMARIZATION/ANALYSIS/REVIEW:
- MCP / Connectors (changed) [source-only]
  - Feature example: setOauthTokenFromFd: () => N11,
  - Feature example: getOauthTokenFromFd: () => Mk6,
- Plugins / Marketplace (changed) [source-only]
  - Command example: return `The name '${A}' is reserved for official Anthropic marketplaces. Only repositories from 'github.com/${Ya1}/' can use this name.`;
  - Feature example: return `The name '${A}' is reserved for official Anthropic marketplaces and can only be used with GitHub sources from the '${Ya1}' organization.`;
- Agents / Skills (changed)
  - Official: Fixed a crash when agent teams setting changed between renders
  - Command example: T.set("User-Agent", "axios/" + n11, !1);
  - Feature example: return new ga1.Agent({ keepAlive: !0, maxSockets: 50, ...w });
- Memory / Context (changed) [source-only]
  - Feature example: propertiesExtractor: (q, K) => ({ signingProperties: { config: q, context: K } }),
  - Feature example: kHTTPContext: Symbol("http context"),
- Permissions / Sandbox (changed)
  - Official: Fixed a bug where commands excluded from sandboxing (via `sandbox.excludedCommands` or `dangerouslyDisableSandbox`) could bypass the Bash ask permission rule when `autoAllowBashIfSandboxed` was enabled
  - Config example: allow: b.array(qa1).optional().describe("List of permission rules for allowed operations"),
  - Config example: deny: b.array(qa1).optional().describe("List of permission rules for denied operations"),
- Models / Effort (changed) [source-only]
  - Command example: { [t8]: ["POST", "/custom-models/create-custom-model", 202] },
  - Command example: { [t8]: ["POST", "/model-customization/custom-model-deployments", 202] },
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: else if (BE("color") || BE("colors") || BE("color=true") || BE("color=always")) Pn1 = 1;
  - Feature example: return IB6(A, "remote", "origin", "url");

## Official Changelog
- Fixed a crash when agent teams setting changed between renders
- Fixed a bug where commands excluded from sandboxing (via `sandbox.excludedCommands` or `dangerouslyDisableSandbox`) could bypass the Bash ask permission rule when `autoAllowBashIfSandboxed` was enabled
