# Claude Code 2.0.53

Compared with `2.0.52`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 471008 -> 471011 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)


## 0. Package Files
- CHANGED: `cli.js` (text, 471008 -> 471011 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

## 1. Slash Commands
### Changes
- No changes detected

### Current Inventory
- Built-in (46): /add-dir, /agents, /clear, /compact, /config, /cost, /doctor, /exit, /export, /extra-usage, /feedback, /files, /help, /hooks, /ide, /init, /install-github-app, /login, /logout, /mcp, /migrate-installer, /model, /output-style, /passes, /permissions, /plugin, /pr-comments, /privacy-settings, /rate-limit-options, /release-notes, /remote-env, /rename, /resume, /review, /rewind, /sandbox, /security-review, /status, /statusline, /stickers, /tasks, /terminal-setup, /todos, /upgrade, /usage, /vim
- Inferred (2): /memory [inferred/low], /plan [inferred/low]

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: let Y = T0A("warning") ?? "Run /compact to compact & continue";
  - Command example: let Y = R0A("warning") ?? "Run /compact to compact & continue";
- MCP / Connectors (changed) [source-only]
  - Feature example: return (u(`Successfully read OAuth token from file descriptor ${B}`), EBA(I), I);
  - Feature example: let K = `Plugin MCP loading error - ${V.type}: ${gL(V)}`;
- Plugins / Marketplace (changed) [source-only]
  - Feature example: let K = `Plugin MCP loading error - ${V.type}: ${gL(V)}`;
  - Feature example: let K = `Plugin MCP server error - ${V.type}: ${gL(V)}`;
- Agents / Skills (changed) [source-only]
  - Command example: R.set("User-Agent", "axios/" + aa, !1);
  - Feature example: "User-Agent": yc(),
- Memory / Context (changed) [source-only]
  - Feature example: (O.context = _QA(null)),
  - Feature example: else if (BM.object(A)) return sH.cache(A.memory, A.files, A.items);
- Permissions / Sandbox (changed) [source-only]
  - Config example: return Ct({ permissionMode: G ?? "default", mainLoopModel: Q, exceeds200kTokens: !1 });
  - Config example: let Y = Ct({ permissionMode: (await B()).mode, mainLoopModel: Q }),
- Models / Effort (changed) [source-only]
  - Config example: description: `Use the default model (currently ${hK6($aA({ forDisplay: !0 }))}) · ${vc(Xt)}`,
  - Feature example: `Opus ${A ? "4.1" : "4.5"} · ${A ? "Legacy" : "Most capable for complex work"} · ${vc(A ? cnA : pnA)}` +
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: let Z = KC1(VC1(A, Yp0[B], "color", ...G), KR.color.close, this[M9A]);
  - Feature example: return b.length >= 2 && xO.test(b[0]) && b[1] === "color: ".concat(Wx(n) || "");

## Official Changelog
- No official changelog entry found for this version
