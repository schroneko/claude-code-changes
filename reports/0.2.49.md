# Claude Code 0.2.49

Compared with `0.2.48`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 228520 -> 228529 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)


## 0. Package Files
- CHANGED: `cli.js` (text, 228520 -> 228529 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

## 1. CLI Commands
### Arguments
- No CLI arguments detected

### Options
- No CLI options detected

### Changes
- No changes detected

### Current Inventory
- - No CLI commands detected

## 2. Slash Commands
### Changes
- No changes detected

### Current Inventory
Built-in (19)
  - /allowed-tools - List all currently allowed tools
  - /bug - Submit feedback about ${x2}
  - /clear - Clear conversation history and free up context
  - /compact - Clear conversation history but keep a summary in context
  - /config - Open config panel
  - /cost - Show the total cost and duration of the current session
  - /doctor - Checks the health of your Claude Code installation
  - /exit - Exit the REPL
  - /help - Show help and available commands
  - /init - Initialize a new CLAUDE.md file with codebase documentation
  - /login
  - /logout - Sign out from your Anthropic account
  - /mcp
  - /pr-comments - Get comments from a GitHub pull request
  - /release-notes - View release notes
  - /review - Review a pull request
  - /terminal-setup
  - /theme - Change the theme (light/dark)
  - /vim - Toggle between vim and emacs editing modes
Plugin-backed (0)
  - none
Inferred (0)
  - none

## 3. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 4. Capability Signals
- MCP / Connectors (changed)
  - Official: Previous MCP server scopes have been renamed: previous "project" scope is now "local" and "global" scope is now "user"
  - Feature example: _b1._addResourceSpans = Cb1;
  - Feature example: let G = xw4.addPerformanceInstrumentationHandler("resource", ({ entries: W }) => {
- Agents / Skills (changed) [source-only]
  - Feature example: class tb1 extends eb1.Agent {
  - Feature example: Ij1.Agent = tb1;
- Memory / Context (changed) [source-only]
  - Feature example: ? BV4(this._latestRoute.context)
  - Feature example: data: { [X8.SEMANTIC_ATTRIBUTE_SENTRY_SOURCE]: Z.context ? DV4(Z.context) : "url" },
- Permissions / Sandbox (changed) [source-only]
  - Config example: d3.PermissionDeniedError = GO;
  - Config example: G2.createElement(m, { bold: !0, color: f.permission }, "Submit Bug Report"),
- Models / Effort (changed) [source-only]
  - Config example: this.model = wv1[w];
  - Config example: (I = Math.max(I || 0, 0)), new p7([...this.color.map(Wm5(I)), this.valpha], this.model)
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: else if (ww("color") || ww("colors") || ww("color=true") || ww("color=always")) uF = 1;
  - Feature example: return q.length >= 2 && Ov.test(q[0]) && q[1] === "color: ".concat(cz(v) || "");

## Official Changelog
- Previous MCP server scopes have been renamed: previous "project" scope is now "local" and "global" scope is now "user"
