# Claude Code 2.1.7

Compared with `2.1.6`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 494367 -> 496575 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

### Slash Commands
- ADDED: `/color` [source-only]

### Environment Variables
- ADDED: `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA`
- REMOVED: `CLAUDE_CODE_AGENT_ID`
- REMOVED: `CLAUDE_CODE_PARENT_SESSION_ID`
- REMOVED: `CLAUDE_CODE_TEAM_NAME`


## 0. Package Files
- CHANGED: `cli.js` (text, 494367 -> 496575 lines)
- CHANGED: `package.json` (text, 32 -> 32 lines)

## 1. Slash Commands
### Changes
- ADDED: `/color` [source-only]

### Current Inventory
- Built-in (57): /add-dir, /agents, /btw, /chrome, /clear, /color, /compact, /config, /context, /cost, /discover, /doctor, /exit, /export, /extra-usage, /feedback, /files, /help, /hooks, /ide, /init, /install-github-app, /install-slack-app, /keybindings, /login, /logout, /mcp, /mobile, /model, /output-style, /passes, /permissions, /plan, /plugin, /privacy-settings, /rate-limit-options, /release-notes, /remote-env, /rename, /resume, /rewind, /sandbox, /skills, /stats, /status, /statusline, /stickers, /tag, /tasks, /terminal-setup, /theme, /think-back, /thinkback-play, /todos, /upgrade, /usage, /vim
- Plugin-backed (3): /code-review, /pr-comments, /security-review
- Inferred (4): /memory [inferred/medium], /review [inferred/medium], /share [inferred/medium], /teleport [inferred/medium]

## 2. Public Surface
### Models
- ADDED: `claude-code-keybindings`

### Environment Variables
- ADDED: `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA`
- REMOVED: `CLAUDE_CODE_AGENT_ID`
- REMOVED: `CLAUDE_CODE_PARENT_SESSION_ID`
- REMOVED: `CLAUDE_CODE_TEAM_NAME`

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed)
  - Official: Fixed spinner briefly flashing when running local slash commands like `/model` or `/theme`
  - Command example: tryItPrompt: "Type /resume to continue a past conversation",
  - Command example: tryItPrompt: "Type /clear for a fresh conversation",
- MCP / Connectors (changed)
  - Official: Enabled MCP tool search auto mode by default for all users. When MCP tool descriptions exceed 10% of the context window, they are automatically deferred and discovered via the MCPSearch tool instead of being loaded upfront. This reduces context usage for users with many MCP tools configured. Users can disable this by adding `MCPSearch` to `disallowedTools` in their settings.
  - Official: Changed OAuth and API Console URLs from console.anthropic.com to platform.claude.com
  - Feature example: return g0.oauthTokenFromFd;
  - Feature example: g0.oauthTokenFromFd = A;
- Plugins / Marketplace (changed) [source-only]
  - Command example: { [HB]: ["POST", "/marketplace-model/endpoints", 200] },
  - Command example: { [HB]: ["DELETE", "/marketplace-model/endpoints/{endpointArn}", 200] },
- Agents / Skills (changed)
  - Official: Added inline display of agent's final response in task notifications, making it easier to see results without reading the full transcript file
  - Command example: _.set("User-Agent", "axios/" + C1A, !1);
  - Feature example: class IKQ extends XKQ.Agent {
- Memory / Context (changed)
  - Official: Added inline display of agent's final response in task notifications, making it easier to see results without reading the full transcript file
  - Official: Fixed context window blocking limit being calculated using the full context window instead of the effective context window (which reserves space for max output tokens)
  - Config example: illegalBy: { msg: $1.message, context: HA.slice(y1 - 100, y1 + 100), mode: $1.mode },
  - Feature example: ((this.props = A), (this.context = Q), (this.refs = N2Q), (this.updater = B || U2Q));
- Permissions / Sandbox (changed)
  - Official: Added ability to provide feedback when accepting permission prompts
  - Official: Fixed security vulnerability where wildcard permission rules could match compound commands containing shell operators
  - Config example: "Permissions-Policy",
  - Config example: let { permissionMode: Q, mainLoopModel: B, exceeds200kTokens: G = !1 } = A;
- Models / Effort (changed)
  - Official: Fixed spinner briefly flashing when running local slash commands like `/model` or `/theme`
  - Command example: { [HB]: ["POST", "/custom-models/create-custom-model", 202] },
  - Command example: { [HB]: ["POST", "/model-customization/custom-model-deployments", 202] },
- IDE / UI / Voice / Remote (changed)
  - Official: Fixed spinner briefly flashing when running local slash commands like `/model` or `/theme`
  - Official: [VSCode] Fixed `claudeProcessWrapper` setting passing the wrapper path instead of the Claude binary path
  - Source: Slash command added: /color
  - Feature example: return p2() && g0.clientType !== "claude-vscode";
  - Feature example: (E.color = B.selectColor(W)),

## Official Changelog
- Added `showTurnDuration` setting to hide turn duration messages (e.g., "Cooked for 1m 6s")
- Added ability to provide feedback when accepting permission prompts
- Added inline display of agent's final response in task notifications, making it easier to see results without reading the full transcript file
- Fixed security vulnerability where wildcard permission rules could match compound commands containing shell operators
- Fixed false "file modified" errors on Windows when cloud sync tools, antivirus scanners, or Git touch file timestamps without changing content
- Fixed orphaned tool_result errors when sibling tools fail during streaming execution
- Fixed context window blocking limit being calculated using the full context window instead of the effective context window (which reserves space for max output tokens)
- Fixed spinner briefly flashing when running local slash commands like `/model` or `/theme`
- Fixed terminal title animation jitter by using fixed-width braille characters
- Fixed plugins with git submodules not being fully initialized when installed
- Fixed bash commands failing on Windows when temp directory paths contained characters like `t` or `n` that were misinterpreted as escape sequences
- Improved typing responsiveness by reducing memory allocation overhead in terminal rendering
- Enabled MCP tool search auto mode by default for all users. When MCP tool descriptions exceed 10% of the context window, they are automatically deferred and discovered via the MCPSearch tool instead of being loaded upfront. This reduces context usage for users with many MCP tools configured. Users can disable this by adding `MCPSearch` to `disallowedTools` in their settings.
- Changed OAuth and API Console URLs from console.anthropic.com to platform.claude.com
- [VSCode] Fixed `claudeProcessWrapper` setting passing the wrapper path instead of the Claude binary path
