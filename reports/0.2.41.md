# Claude Code 0.2.41

Compared with `0.2.40`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 227147 -> 227240 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)


## 0. Package Files
- CHANGED: `cli.js` (text, 227147 -> 227240 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

## 1. Slash Commands
### Changes
- No changes detected

### Current Inventory
- Built-in (19): /allowed-tools, /bug, /clear, /compact, /config, /cost, /doctor, /exit, /help, /init, /login, /logout, /mcp, /pr-comments, /release-notes, /review, /terminal-setup, /theme, /vim

## 2. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 3. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: G > 0 ? `Found ${G} MCP server${G !== 1 ? "s" : ""} (use /mcp for status)` : "",
  - Command example: "Context low · Run /compact to compact & continue",
- MCP / Connectors (changed)
  - Official: MCP server startup timeout can now be configured via MCP_TIMEOUT environment variable
  - Official: MCP server startup no longer blocks the app from starting up
  - Feature example: mT1._addResourceSpans = uT1;
  - Feature example: let Z = xB4.addPerformanceInstrumentationHandler("resource", ({ entries: W }) => {
- Agents / Skills (changed) [source-only]
  - Feature example: class Rb1 extends Ub1.Agent {
  - Feature example: fb1.Agent = Rb1;
- Memory / Context (changed) [source-only]
  - Feature example: ? Bw4(this._latestRoute.context)
  - Feature example: data: { [W8.SEMANTIC_ATTRIBUTE_SENTRY_SOURCE]: G.context ? Jw4(G.context) : "url" },
- Permissions / Sandbox (changed) [source-only]
  - Config example: permission: E8(G.permission),
  - Config example: H5.PermissionDeniedError = iy;
- Models / Effort (changed) [source-only]
  - Config example: this.model = Vf1[w];
  - Config example: let B = a01[this.model][Z];
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: let I = lk.chrome,
  - Feature example: else if (tB("color") || tB("colors") || tB("color=true") || tB("color=always")) yF = 1;

## Official Changelog
- MCP server startup timeout can now be configured via MCP_TIMEOUT environment variable
- MCP server startup no longer blocks the app from starting up
