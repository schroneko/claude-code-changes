# Claude Code 0.2.36

Compared with `0.2.35`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 219780 -> 220430 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)


## 0. Package Files
- CHANGED: `cli.js` (text, 219780 -> 220430 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

## 1. CLI Commands
### Arguments
- No CLI arguments detected

### Options
- No CLI options detected

### Changes
- No changes detected

### Current Inventory
- - No CLI commands detected

## 2. Slash Commands
### Changes
- No changes detected

### Current Inventory
Built-in (18)
  - /approved-tools - List all currently approved tools
  - /bug - Submit feedback about ${j2}
  - /clear - Clear conversation history and free up context
  - /compact - Clear conversation history but keep a summary in context
  - /config - Open config panel
  - /cost - Show the total cost and duration of the current session
  - /doctor - Checks the health of your Claude Code installation
  - /exit - Exit the REPL
  - /help - Show help and available commands
  - /init - Initialize a new CLAUDE.md file with codebase documentation
  - /login
  - /logout - Sign out from your Anthropic account
  - /mcp
  - /pr-comments - Get comments from a GitHub pull request
  - /release-notes - Show release notes for the current or specified version
  - /review - Review a pull request
  - /terminal-setup
  - /vim - Toggle between vim and emacs editing modes
Plugin-backed (0)
  - none
Inferred (0)
  - none

## 3. Public Surface
### Models
- ADDED: `claude-desktop`

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 4. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: "% remaining) · Run /compact to compact & continue",
  - Command example: "Use the /compact command to summarize older conversation history while preserving recent messages.",
- MCP / Connectors (changed)
  - Official: Import MCP servers from Claude Desktop with `claude mcp add-from-claude-desktop`
  - Official: Add MCP servers as JSON strings with `claude mcp add-json <n> <json>`
  - Feature example: uO1._addResourceSpans = PO1;
  - Feature example: let Z = LI4.addPerformanceInstrumentationHandler("resource", ({ entries: d }) => {
- Agents / Skills (changed) [source-only]
  - Feature example: class qm1 extends Nm1.Agent {
  - Feature example: gm1.Agent = qm1;
- Memory / Context (changed) [source-only]
  - Feature example: [Xw.SEMANTIC_ATTRIBUTE_SENTRY_SOURCE]: this._latestRoute.context
  - Feature example: ? aI4(this._latestRoute.context)
- Permissions / Sandbox (changed) [source-only]
  - Config example: "The requested file could not be read, typically due to permission problems that have occurred after a reference to a file was acquired.",
  - Config example: throw new Error("At least one permission should be defined in access boundary rules.");
- Models / Effort (changed) [source-only]
  - Config example: return { model: d, value: Z };
  - Config example: ((this.model = I.model), (this.color = [...I.color]), (this.valpha = I.valpha));
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: let I = pj.chrome,
  - Feature example: else if (lW("color") || lW("colors") || lW("color=true") || lW("color=always")) RH = 1;

## Official Changelog
- Import MCP servers from Claude Desktop with `claude mcp add-from-claude-desktop`
- Add MCP servers as JSON strings with `claude mcp add-json <n> <json>`
