# Claude Code 2.1.74

Compared with `2.1.73`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 546516 -> 548770 lines)
- CHANGED: `package.json` (text, 33 -> 33 lines)
- CHANGED: `sdk-tools.d.ts` (text, 2828 -> 2832 lines)

### Environment Variables
- ADDED: `CLAUDE_CODE_FRAME_TIMING_LOG`
- ADDED: `CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS`
- REMOVED: `CLAUDE_CODE_TWO_STAGE_CLASSIFIER`


## 0. Package Files
- CHANGED: `cli.js` (text, 546516 -> 548770 lines)
- CHANGED: `package.json` (text, 33 -> 33 lines)
- CHANGED: `sdk-tools.d.ts` (text, 2828 -> 2832 lines)

## 1. CLI Commands
- No changes detected

### CLI Arguments
- No changes detected

### CLI Options
- CHANGED: `claude plugin disable [plugin] -s, --scope <scope>` (description changed)
- CHANGED: `claude plugin enable <plugin> -s, --scope <scope>` (description changed)
- CHANGED: `claude plugin update <plugin> -s, --scope <scope>` (description changed)

## 2. Slash Commands
- No changes detected

## 3. Public Surface
### Models
- ADDED: `claude-code-feedback`

### Environment Variables
- ADDED: `CLAUDE_CODE_FRAME_TIMING_LOG`
- ADDED: `CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS`
- REMOVED: `CLAUDE_CODE_TWO_STAGE_CLASSIFIER`

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 4. Capability Signals
- Slash / Workflows (changed)
  - Official: Fixed `/plugin install` failing inside the REPL for marketplace plugins with local sources
  - Official: Fixed unknown slash commands with arguments silently dropping input — now shows your input as a warning
  - Command example: BK.createElement(T, { dimColor: !0 }, "/plan to preview"),
  - Command example: z && T3.createElement(T, { dimColor: !0 }, "Plan saved to: ", w, " · /plan to edit"),
- MCP / Connectors (changed)
  - Official: Fixed MCP OAuth authentication hanging when the callback port is already in use
  - Official: Fixed MCP OAuth refresh never prompting for re-auth after the refresh token expires, for OAuth servers that return errors with HTTP 200 (e.g. Slack)
  - Feature example: setOauthTokenFromFd: () => L86,
  - Feature example: getOauthTokenFromFd: () => $x1,
- Plugins / Marketplace (changed)
  - Official: Fixed `/plugin install` failing inside the REPL for marketplace plugins with local sources
  - Official: Fixed marketplace update not syncing git submodules — plugin sources in submodules no longer break after update
  - Config example: _T3 = "DeregisterMarketplaceModelEndpoint",
  - Command example: { [nA]: ["POST", "/marketplace-model/endpoints", 200] },
- Agents / Skills (changed)
  - Official: Fixed managed policy `ask` rules being bypassed by user `allow` rules or skill `allowed-tools`
  - Official: Fixed full model IDs (e.g., `claude-opus-4-5`) being silently ignored in agent frontmatter `model:` field and `--agents` JSON config — agents now accept the same model values as `--model`
  - Command example: V.set("User-Agent", "axios/" + VA6, !1);
  - Command example: headers: { "Content-Type": "application/json", "User-Agent": pO(), ...K.headers },
- Memory / Context (changed)
  - Official: Added actionable suggestions to `/context` command — identifies context-heavy tools, memory bloat, and capacity warnings with specific optimization tips
  - Official: Added `autoMemoryDirectory` setting to configure a custom directory for auto-memory storage
  - Feature example: sampling: Q7({ context: $P.optional(), tools: $P.optional() }).optional(),
  - Feature example: context: Q7({ arguments: Xw(B1(), B1()).optional() }).optional(),
- Permissions / Sandbox (changed)
  - Official: Fixed voice mode silently failing on the macOS native binary for users whose terminal had never been granted microphone permission — the binary now includes the `audio-input` entitlement so macOS prompts correctly
  - Config example: PERMISSION_DENIED: GZA,
  - Config example: PERMISSION_MODES: () => LW,
- Models / Effort (changed)
  - Official: Fixed full model IDs (e.g., `claude-opus-4-5`) being silently ignored in agent frontmatter `model:` field and `--agents` JSON config — agents now accept the same model values as `--model`
  - Config example: model: B1(),
  - Command example: { [nA]: ["POST", "/custom-models/create-custom-model", 202] },
- IDE / UI / Voice / Remote (changed)
  - Official: Fixed voice mode silently failing on the macOS native binary for users whose terminal had never been granted microphone permission — the binary now includes the `audio-input` entitlement so macOS prompts correctly
  - Official: [VSCode] Fixed delete button not working for Untitled sessions
  - Feature example: return w7() && v1.clientType !== "claude-vscode";
  - Feature example: theme: KZ(["light", "dark"]).optional(),

## Official Changelog
- Added actionable suggestions to `/context` command — identifies context-heavy tools, memory bloat, and capacity warnings with specific optimization tips
- Added `autoMemoryDirectory` setting to configure a custom directory for auto-memory storage
- Fixed memory leak where streaming API response buffers were not released when the generator was terminated early, causing unbounded RSS growth on the Node.js/npm code path
- Fixed managed policy `ask` rules being bypassed by user `allow` rules or skill `allowed-tools`
- Fixed full model IDs (e.g., `claude-opus-4-5`) being silently ignored in agent frontmatter `model:` field and `--agents` JSON config — agents now accept the same model values as `--model`
- Fixed MCP OAuth authentication hanging when the callback port is already in use
- Fixed MCP OAuth refresh never prompting for re-auth after the refresh token expires, for OAuth servers that return errors with HTTP 200 (e.g. Slack)
- Fixed voice mode silently failing on the macOS native binary for users whose terminal had never been granted microphone permission — the binary now includes the `audio-input` entitlement so macOS prompts correctly
- Fixed `SessionEnd` hooks being killed after 1.5 s on exit regardless of `hook.timeout` — now configurable via `CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS`
- Fixed `/plugin install` failing inside the REPL for marketplace plugins with local sources
- Fixed marketplace update not syncing git submodules — plugin sources in submodules no longer break after update
- Fixed unknown slash commands with arguments silently dropping input — now shows your input as a warning
- Fixed Hebrew, Arabic, and other RTL text not rendering correctly in Windows Terminal, conhost, and VS Code integrated terminal
- Fixed LSP servers not working on Windows due to malformed file URIs
- Changed `--plugin-dir` so local dev copies now override installed marketplace plugins with the same name (unless that plugin is force-enabled by managed settings)
- [VSCode] Fixed delete button not working for Untitled sessions
- [VSCode] Improved scroll wheel responsiveness in the integrated terminal with terminal-aware acceleration
