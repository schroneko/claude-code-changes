# Claude Code 0.2.40

Compared with `0.2.39`

## Source-Only Highlights
### Package Files
- CHANGED: `cli.js` (text, 227076 -> 227147 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

### Slash Commands
- ADDED: `/allowed-tools` [source-only]


## 0. Package Files
- CHANGED: `cli.js` (text, 227076 -> 227147 lines)
- CHANGED: `package.json` (text, 28 -> 28 lines)

## 1. CLI Commands
### Arguments
- No CLI arguments detected

### Options
- No CLI options detected

### Changes
- No changes detected

### Current Inventory
- - No CLI commands detected

## 2. Slash Commands
### Changes
- ADDED: `/allowed-tools` [source-only]
- REMOVED: `/approved-tools`

### Current Inventory
Built-in (19)
  - /allowed-tools - List all currently allowed tools
  - /bug - Submit feedback about ${j2}
  - /clear - Clear conversation history and free up context
  - /compact - Clear conversation history but keep a summary in context
  - /config - Open config panel
  - /cost - Show the total cost and duration of the current session
  - /doctor - Checks the health of your Claude Code installation
  - /exit - Exit the REPL
  - /help - Show help and available commands
  - /init - Initialize a new CLAUDE.md file with codebase documentation
  - /login
  - /logout - Sign out from your Anthropic account
  - /mcp
  - /pr-comments - Get comments from a GitHub pull request
  - /release-notes - View release notes
  - /review - Review a pull request
  - /terminal-setup
  - /theme - Change the theme (light/dark)
  - /vim - Toggle between vim and emacs editing modes
Plugin-backed (0)
  - none
Inferred (0)
  - none

## 3. Public Surface
### Models
- No changes detected

### Environment Variables
- No changes detected

### Settings
- No changes detected

### SDK Tools
- No changes detected

## 4. Capability Signals
- Slash / Workflows (changed) [source-only]
  - Command example: "Custom slash commands: Markdown files in .claude/commands/ directories now appear as custom slash commands to insert prompts into your conversation",
  - Feature example: "Fixed issue where slash command arguments weren't being sent properly",
- MCP / Connectors (changed) [source-only]
  - Feature example: ym1._addResourceSpans = Lm1;
  - Feature example: let Z = MB4.addPerformanceInstrumentationHandler("resource", ({ entries: W }) => {
- Agents / Skills (changed) [source-only]
  - Feature example: class gb1 extends qb1.Agent {
  - Feature example: Nb1.Agent = gb1;
- Memory / Context (changed) [source-only]
  - Feature example: [UV.SEMANTIC_ATTRIBUTE_SENTRY_SOURCE]: this._latestRoute.context
  - Feature example: ? aB4(this._latestRoute.context)
- Permissions / Sandbox (changed) [source-only]
  - Config example: permission: R8(G.permission),
  - Command example: "New /approved-tools command for managing tool permissions",
- Models / Effort (changed) [source-only]
  - Config example: (W = JB[this.model].channels),
  - Config example: ((this.model = G || "rgb"), (W = JB[this.model].channels));
- IDE / UI / Voice / Remote (changed) [source-only]
  - Feature example: else if (tB("color") || tB("colors") || tB("color=true") || tB("color=always")) PF = 1;
  - Feature example: return q.length >= 2 && Rv.test(q[0]) && q[1] === "color: ".concat(OK(S) || "");

## Official Changelog
- No official changelog entry found for this version
